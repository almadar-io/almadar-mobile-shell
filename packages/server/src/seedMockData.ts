/**
 * Mock data seeding — no-op placeholder for mobile shell.
 */

export async function initializeMockData(): Promise<void> {
  // Intentionally empty; real mock seeding lives in generated projects.
}
