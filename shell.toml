[shell]
name = "mobile"
description = "React Native mobile application shell"
target = "react-native"
platforms = ["ios", "android"]

[packages]
client = "mobile"
server = "server"
shared = "shared"

[compiler]
generated_dir = "packages/mobile/src/generated"
